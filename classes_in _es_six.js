class Point {
    constructor(a, b) {
        this.x = a;

        this.y = b;
    }
    getX() { return this.x; }

    getY() { return this.y; }
}
//to declare a class Point with a constructor initializing its x and y members.